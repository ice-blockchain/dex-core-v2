;; gas constants
const gas::router::route = 81000000;
const gas::router::pay_to = 64000000;
const gas::router::vault_pay_to = 80000000;
const gas::router::pay_vault = 80000000;
const gas::router::set_fees = 80000000;
const gas::router::reset_pool_gas = 80000000;
const gas::router::update_pool_status = 80000000;
const gas::router::set_params = 80000000;
const gas::router::update_pool_code = 80000000;
<% if (bondingCurve) {
print(`const gas::router::set_bonding_curve_fees = 80000000;`);
print(`const gas::router::set_bonding_curve_price = 80000000;`);
} %>

const gas::pool::cb_refund = 60000000;
const gas::pool::provide_lp = 64000000;
const gas::pool::collect_fees = 80000000;
const gas::pool::provide_wallet_address = 80000000;
const gas::pool::set_fees = 80000000;
const gas::pool::reset_gas = 80000000;
const gas::pool::internal_update_status = 80000000;
const gas::pool::internal_set_params = 80000000;
const gas::pool::internal_update_pool_code = 80000000;

const gas::vault::deposit_ref_fee = 40000000;
const gas::vault::withdraw_fee = 40000000;

const gas::lp_account::refund = 40000000;
const gas::lp_account::direct_add_liquidity = 40000000;
const gas::lp_account::add_liquidity = 40000000;

const gas::lp_wallet::burn = 80000000;
const gas::lp_wallet::mint = 80000000;

const gas::jetton_wallet::est_transfer = 280000000; ;; 0.28 TON

;; PUBLIB enabled conf
const storage_fee::router            = 80000000; ;; 0.080 TON
const storage_fee::pool              = 40000000; ;; 0.040 TON
const storage_fee::lp_account        = 4000000;  ;; 0.004 TON
const storage_fee::lp_wallet         = 4000000;  ;; 0.004 TON
const storage_fee::est_jetton_wallet = 40000000; ;; 0.040 TON
const storage_fee::vault             = 4000000;  ;; 0.004 TON

{-
;; use following conf if PUBLIB *NOT* enabled
;; NOTE: fees will be much more higher

const storage_fee::router            = 4000000000; ;; 4.00 TON
const storage_fee::pool              = 400000000;  ;; 0.40 TON
const storage_fee::lp_account        = 40000000;   ;; 0.04 TON
const storage_fee::lp_wallet         = 40000000;   ;; 0.04 TON
const storage_fee::est_jetton_wallet = 40000000;   ;; 0.04 TON
const storage_fee::vault             = 40000000;   ;; 0.04 TON
-}
